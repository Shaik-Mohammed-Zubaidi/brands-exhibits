import graphImage from './graph.PNG';

const Graph = () =>{
    return (
        <div className="graph" >
            <img src={graphImage} alt="graph" />
        </div>
    );
};

export default Graph;